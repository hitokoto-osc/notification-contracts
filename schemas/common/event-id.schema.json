{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "EventID",
  "description": "可选的事件身份片段，混入各业务消息。",
  "type": "object",
  "properties": {
    "event_id": {
      "type": "string",
      "format": "uuid",
      "description": "可选。事件的不可变身份，语义对齐 CloudEvents 的 `id`。\n\n- 推荐 UUIDv7：自带时间序，消费侧可按前缀清理去重桶。\n- 同一逻辑事件**重发时必须复用同一值**，否则消费侧去重失效。\n- 与 `poll_created` / `poll_finished` 中的 `id`（整数投票标识）是两回事，不要混用。",
      "examples": [
        "0191d9c6-6f7c-7c3a-9f4e-3f5b2c1d8a90"
      ]
    }
  }
}
